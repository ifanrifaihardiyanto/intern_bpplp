const requestUrl = `/bright/hero`;

const heroRequests = {
  getHeroList: `${requestUrl}/list`,
  getHeroInsight: `${requestUrl}/insight`,
  getHeroRecommendation: `${requestUrl}/recommendation`,
  getHeroAchievement: `${requestUrl}/achievement`,
};

export { heroRequests };
